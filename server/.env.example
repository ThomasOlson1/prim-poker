# Server Configuration
PORT=8080

# Smart Contract Integration (Optional - server works without these)
# RPC URL for the blockchain network (Base Mainnet or Base Sepolia)
RPC_URL=https://mainnet.base.org
# RPC_URL=https://sepolia.base.org  # For testnet

# Deployed Poker Contract Address
POKER_CONTRACT_ADDRESS=

# Game Server Private Key (used to sign transactions for startNewHand, distributeWinnings, etc.)
# ⚠️  IMPORTANT: This should be a dedicated game server wallet with limited funds
# This wallet must be authorized as the game server in the contract via setGameServer()
GAME_SERVER_PRIVATE_KEY=

# CORS Configuration (comma-separated origins)
CORS_ORIGINS=http://localhost:3000,https://yourdomain.com
